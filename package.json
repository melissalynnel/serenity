{
  "name": "serenity",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build:portfolio": "npm --prefix portfolio run build",
    "build:artemis": "rm -rf docs/artemis && mkdir -p docs/artemis && cp -R artemis/* docs/artemis/",
    "build:pages": "npm run build:portfolio && npm run build:artemis"
  }
}
